<button
  type="<%= local_assigns.fetch(:type, "submit") %>"
  class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-red-600 hover:bg-red-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500"
  data-controller="<%= local_assigns[:controller] %>"
  data-action="<%= local_assigns[:action] %>"
>
  <%= local_assigns[:text] || "Button" %>
</button>